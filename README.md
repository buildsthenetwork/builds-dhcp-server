# Build's DHCP Server

## Currently, a fun project to build a DHCP server
- Better understand the DHCP Protocol
- Create a service that serves DHCP functionality
- Implement RESTful API endpoints to interact with the server in a scalable manner

## Languages
- Looking to prioritize the DHCP service in Go
- API can be done in Node or Python. Maybe Go?
- DHCP client can be written in React
